[project]
name = "transactions-dryrun-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rickard Aaberg", email = "rickard.aaberg@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
]

[project.scripts]
transactions-dryrun-1 = "transactions_dryrun_1:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
